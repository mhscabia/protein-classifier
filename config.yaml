data:
  raw_path: "data/raw/"
  processed_path: "data/processed/"
  uniprot_limit: 500
  go_namespace: "molecular_function"

model:
  random_seed: 42
  test_size: 0.2

output:
  path: "data/output/"

logging:
  level: "INFO"
